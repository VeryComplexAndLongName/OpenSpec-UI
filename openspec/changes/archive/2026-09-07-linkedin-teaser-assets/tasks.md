# Tasks

The paths and the format below were corrected on 2026-09-07, before
archiving, against what is actually in the repository. The tasks were
written for four SVGs under `docs/images/linkedin-teaser/`; what shipped
in #285 is four PNGs under `docs/images/standalone/`, and that directory
does not exist. The ticks were true about the work and wrong about where
it landed, which is the form this repository's own record has gone wrong
before — so it is corrected against the tree rather than left to read as
if a folder existed.

## Assets

- [x] 1.1 Add `docs/images/standalone/01-permission-request.png` showing
  a permission request with Allow and Deny controls and a waiting stage.
- [x] 1.2 Add `docs/images/standalone/02-human-only-inbox.png` showing
  open human-only items across several named changes without a Done control.
- [x] 1.3 Add `docs/images/standalone/03-observation-record.png` showing
  an incorrectly closed human-only item reopened until a person observes it.
- [x] 1.4 Add `docs/images/standalone/04-mechanical-checks.png` showing
  the named-successor and spec-delta checks failing with stated reasons.

## Verification

- [x] 2.1 **Human-only**: open all four images and confirm their text,
  layout, and visual legibility at the target LinkedIn preview size.
- [x] 2.2 Confirm all four image files are present under
  `docs/images/standalone/`. Verified 2026-09-07 against the tree:
  `01-permission-request.png`, `02-human-only-inbox.png`,
  `03-observation-record.png`, `04-mechanical-checks.png`.
