{
  "name": "openspec-ui",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "engines": {
    "node": ">=22 <23",
    "npm": ">=10 <11"
  },
  "packageManager": "npm@10.9.0",
  "volta": {
    "node": "22.11.0",
    "npm": "10.9.0"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "verify": "npm run typecheck && npm run lint && npm run test",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "lint": "npm run lint:english && npm run lint --workspaces --if-present",
    "lint:english": "node scripts/check-english.mjs",
    "test": "npm run test:english && npm run test --workspaces --if-present",
    "test:english": "node --test scripts/check-english.test.mjs",
    "changeset": "changeset",
    "changeset:version": "changeset version"
  },
  "devDependencies": {
    "@changesets/cli": "^3.0.1",
    "@eslint/js": "^9.39.5",
    "eslint": "^9.39.5",
    "globals": "^17.11.0",
    "typescript": "^6.0.3",
    "typescript-eslint": "^8.68.0",
    "vitest": "^2.0.0"
  }
}
