---
"@openspec-ui/core": patch
"@openspec-ui/webui": patch
"openspec-ui-vscode": patch
---

One entry starts a run, and it shows what the configuration resolves to
before starting: the path, each stage's agent, any ceiling that cannot
act, and the recommended configuration with its grounds. The VS Code
agent becomes a choice inside it rather than a second menu entry.
