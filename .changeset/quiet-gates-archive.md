---
"@openspec-ui/core": patch
---

Preserve the per-change git-stage gate across OpenSpec archive so an agent-sufficient chain reaches push, pull-request checks, and merge after the active change directory is moved.